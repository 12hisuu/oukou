apiVersion: v1
kind: Service
metadata:
  name: vbnxc
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vbnxc"
    port: 8080
  selector:
    app: vbnxc
